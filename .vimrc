" To use this file, add the following two lines into your vimrc:
"     set exrc
"     set secure

filetype plugin off     " Need to disable filetype-specific plugins (will override this file)
set shiftwidth=4
set tabstop=8
set expandtab
set textwidth=100
set formatoptions=tcq
