liblibos {
  GLIBC_2.12 {
    # checkpoint.c
    checkpoint;

    # sandbox.c
    sandbox_create; sandbox_attach; sandbox_current;

    # msgpersist.c
    msgpersist;

    # benchmark.c
    benchmark_rpc; send_rpc; recv_rpc;
  }
}
